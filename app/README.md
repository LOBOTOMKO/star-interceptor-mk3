# app/

Android control application (Kotlin, XML UI, built in Android Studio).

- target selection and manual control
- computes celestial positions via the bundled Python (`calc/`) through Chaquopy
- sends orientations to the mount over Bluetooth Low Energy

**Add here:** the Android Studio project (module sources, `build.gradle`, resources). Build artifacts are ignored by `.gitignore`.
