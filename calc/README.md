# calc/

Celestial-position calculation (Python, `ephem`).

Given an observer's latitude, longitude, elevation and the current time, computes a target body's azimuth, altitude and magnitude — and produces the sequence of upcoming orientations used for autonomous tracking.

**Add here:** the Python source and a `requirements.txt` (e.g. `ephem`).
