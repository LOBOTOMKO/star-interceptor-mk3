# hardware/

Electronics design — custom PCB carrying the ESP32, stepper driver, buck converters and protection.

Designed in KiCad. **Note:** the original KiCad source files were lost; the schematic and board-layout images in `docs/images/` (`schematic.png`, `pcb-render.png`) are the surviving record. If the `.kicad_pro` / `.kicad_sch` / `.kicad_pcb` files are recovered, add them here.

**Add here:** any recovered KiCad project files and exported gerbers.
